# Compiled binaries / build output
bin/
dist/
*.exe
*.dll
*.so
*.dylib

# Root-level `go build ./cmd/...` outputs (anchored so packages keep their dirs)
/controlplane
/ironctl
/mcp-sample
/sandbox

# Go
*.test
*.out
/vendor/
go.work.sum

# Local env / secrets — never commit
.env
*.local
*.key
*.pem

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
.bin/*
# gstack browse session cache
.gstack/

# Local agent / session state — never commit
.claude/
.remember/
