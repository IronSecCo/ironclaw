// OWNER: AGENT1

package parity

import "testing"

// TestDeliveryDedup asserts the behavioral contract: an outbound message is
// delivered exactly once, with dedup recorded in the inbound `delivered` table.
func TestDeliveryDedup(t *testing.T) {
	t.Skip("AGENT1: implement delivery-dedup parity spec")
}
